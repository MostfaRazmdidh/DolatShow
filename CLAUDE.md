# دولت شو (Dolat Show) — راهنمای پروژه برای Claude Code

این فایل رو قبل از هر کاری کامل بخون. این پروژه ماه‌هاست با کمک یه نسخه‌ی دیگه از Claude (تو چت claude.ai) قدم‌به‌قدم ساخته شده، و این سند خلاصه‌ی کامل تصمیم‌ها، معماری، و وضعیت فعلیه — تا لازم نباشه از صفر حدس بزنی یا معماری رو عوض کنی.

**مهم:** توسعه‌دهنده‌ی این پروژه در یونیتی مبتدیه ولی برنامه‌نویسی بلده. توضیحات رو ساده و قدم‌به‌قدم بده، فرض نکن با اصطلاحات ادیتور یونیتی آشناست، ولی نیازی به توضیح مفاهیم پایه‌ی برنامه‌نویسی (متغیر، تابع، کلاس) نیست.

---

## ۱. معرفی بازی

**نام:** دولت شو
**سبک:** کارتی/تصمیم‌گیری سریع (الهام از Reigns)، شبیه‌ساز مدیریت کشور
**پلتفرم:** اندروید، برای انتشار در کافه‌بازار و مایکت (نه گوگل‌پلی — پس فایل خروجی باید APK باشه، نه AAB)
**زبان بازی:** کاملاً فارسی (چالش فنی مهمی داره، پایین‌تر توضیح دادم)

### ایده‌ی اصلی
بازیکن نقش رئیس‌جمهور رو ۴۸ ماه (۴ سال) بازی می‌کنه. هر ماه یه کارت (تصمیم/بحران/گزارش) میاد، بازیکن با سوایپ راست (تایید) یا چپ (رد) تصمیم می‌گیره. هر تصمیم روی ۴ شاخص اثر می‌ذاره. اگه شاخصی به ۰ یا ۱۰۰ برسه، بازی می‌بازه. اگه ۴۸ ماه رو کامل کنه، می‌بره.

### ۴ شاخص (در enum به اسم StatType در GameStats.cs)
- **Budget** (بودجه و اقتصاد)
- **Popularity** (رضایت و محبوبیت مردمی)
- **Security** (امنیت داخلی)
- **Diplomacy** (دیپلماسی و روابط بین‌الملل)

همه بین ۰ تا ۱۰۰، شروع از ۵۰.

### تصمیم مهم درباره‌ی داستان (تازه گرفته شده)
توسعه‌دهنده تصمیم گرفته بازی فقط یه مکانیک ساده نباشه، بلکه **داستان و شخصیت‌های ماندگار** داشته باشه. به همین خاطر:
- سیستم کارت‌ها به‌جای زنجیره‌ی ساده‌ی GDD اولیه (کارت X که N ماه بعد دوباره میاد)، یه **سیستم پرچم (flags)** داره — هر تصمیم می‌تونه یه رشته (string) به‌عنوان پرچم ست کنه، و کارت‌های بعدی می‌تونن شرط‌شون این باشه که یه پرچم خاص ست شده باشه. این امکان خط‌های داستانی موازی و شاخه‌دار رو می‌ده.
- **زمان‌بندی:** ساخت «موتور بازی» (سیستم‌ها، کد) قراره تو حدود ۱ ماه تموم بشه، ولی **نوشتن خودِ محتوای داستان** (شخصیت‌ها، متن ۱۰۰+ کارت) یه کار جداست که می‌تونه بدون عجله و موازی/بعد از این ادامه پیدا کنه. موقع کار روی محتوا عجله نکن و پیشنهاد نده که محتوا رو سرسری پر کنیم فقط برای تکمیل کدنویسی.

---

## ۲. تصمیم‌های فنی مهم و چراشون

### ۲.۱. چرا کارت به‌صورت Sprite هست نه UI؟
کارت (Card) یه GameObject با SpriteRenderer + BoxCollider2D + اسکریپت CardSwipe.cs هست، نه یه المان Canvas UI. علتش اینه که مکانیک سوایپ با `OnMouseDown/OnMouseDrag/OnMouseUp` پیاده شده که نیاز به Collider معمولی داره، نه UI Event System.

**عارضه‌ی این تصمیم:** چون RTLTextMeshPro (برای متن فارسی) یه کامپوننت UI هست و باید زیر Canvas باشه، مجبور شدیم یه **Canvas با Render Mode: World Space** به‌عنوان فرزند (child) خودِ آبجکت Card بسازیم، و AdvisorText و BodyText رو زیر همون Canvas بذاریم. این Canvas باید Scale خیلی کوچیکی داشته باشه (حدود ۰.۰۱، بسته به Scale خود Card ممکنه فرق کنه) تا اندازه‌ش با کارت جور دربیاد.

اگه بخوای تعداد کارت‌ها رو زیاد کنی (مثلاً برای Object Pooling)، این ساختار (Sprite + World Space Canvas تودرتو) باید برای هر کارت تکرار بشه — شاید بهتر باشه یه Prefab ازش بسازی.

### ۲.۲. چالش متن فارسی (خیلی مهم، وقت زیادی صرفش شده)
یونیتی/TextMeshPro به‌صورت پیش‌فرض از فارسی/عربی پشتیبانی نمی‌کنه (حروف نمی‌چسبن، جهت درست نمی‌شه). راه‌حلی که استفاده شده:
- پکیج **RTLTMPro** (از `https://github.com/hk1ll3r/RTLTMPro`، نصب از پوشه‌ی UPMPackage) نصب شده. کامپوننت اصلیش `RTLTextMeshPro` (namespace: `RTLTMPro`) هست که خودش real-time متن رو reshape/reorder می‌کنه — نیازی به دستکاری دستی رشته‌ها نیست.
- برای فونت، بعد از چند بار تلاش ناموفق با ساخت دستی Font Asset از فونت Vazirmatn (مشکل: Character Sequence با فرمت رنج `-` خطای CS0103/FormatException می‌داد؛ باید فرمت `0020-007E,0600-06FF,...` بدون `0x` باشه)، در نهایت از یه TMP Font Asset آماده به اسم **NotoNaskhArabic-Regular SDF** استفاده شد که جواب داد و همه‌ی Presentation Forms (حروف چسبیده) رو داره.
- روی هر RTLTextMeshPro، فیلد **Font Asset** باید NotoNaskhArabic-Regular SDF باشه (نه فونت پیش‌فرض TMP، نه Vazirmatn SDF ناقص).
- اگه بخوای فونت رو عوض کنی یا فونت جدید اضافه کنی، حتماً باید Character Set شامل رنج `FB50-FDFF` و `FE70-FEFF` (Arabic Presentation Forms) باشه وگرنه حروف چسبیده به‌صورت باکس خالی (□) نمایش داده می‌شن.

### ۲.۳. Active Input Handling
پروژه از `Input.mousePosition` (سیستم قدیمی) تو CardSwipe.cs استفاده می‌کنه. باید تو Edit → Project Settings → Player → Active Input Handling روی **Both** باشه (نه فقط Input System Package جدید)، وگرنه InvalidOperationException می‌ده.

### ۲.۴. مسیر پروژه باید انگلیسی باشه
یه بار کل پروژه رو تو مسیری با حروف فارسی ساخته بودیم و باعث شد Package Manager هنگ کنه (احتمالاً مشکل Unicode path تو ابزارهای زیرین مثل npm/git). **همیشه مسیر پروژه باید کاملاً انگلیسی، بدون فاصله باشه.**

### ۲.۵. تنظیمات Android
- Default Orientation: **Portrait** (بازی عمودیه، سبک سوایپ‌کارتی مثل Reigns)
- Build App Bundle (Google Play): **غیرفعال** — چون مقصد کافه‌بازار/مایکته، فقط APK لازمه
- Package Name: فرمت `com.CompanyName.DolatShow`
- Minimum API Level: حدود API 24 (Android 7.0) پیشنهاد شده بود

---

## ۳. معماری کد فعلی

همه‌ی اسکریپت‌ها تو `Assets/Script` هستن.

### GameStats.cs — Singleton، مدیر مرکزی وضعیت بازی
- ۴ شاخص (Budget, Popularity, Security, Diplomacy)، `int`، بین ۰-۱۰۰، شروع از ۵۰ (`startValue`, قابل تنظیم تو Inspector)
- `ApplyEffect(StatType, int amount)` — تغییر یه شاخص با clamp خودکار
- `CurrentMonth` — شمارنده‌ی ماه، شروع از ۱
- `AdvanceMonth()` — بعد از هر تصمیم صدا زده می‌شه، ماه رو جلو می‌بره
- `TotalMonths` = 48 (قابل تنظیم)
- `IsGameOver` — bool، وقتی true شد دیگه نباید کارت جدید بیاد
- Eventها: `OnStatChanged`, `OnStatCritical`, `OnMonthChanged`, `OnGameWon`, `OnGameLost`
- منطق پایان بازی: اگه شاخصی به ۰/۱۰۰ برسه یا ماه از ۴۸ رد بشه → `IsGameOver = true` و event مربوطه صدا زده می‌شه. **فعلاً هیچ UI پایان بازی ساخته نشده** — فقط Debug.Log و غیرفعال کردن کارت. این باید تو فاز بعدی (صفحات پایان بازی) ساخته بشه.

### CardData.cs — ScriptableObject برای هر کارت
فیلدها: `cardId`, `advisorName`, `cardText` (TextArea), `category` (enum: Normal/Crisis/Chain/Special), `approveEffects` و `rejectEffects` (لیست `StatEffect` که هرکدوم `StatType` + `int amount` داره)، و سیستم پرچم: `requiredFlag` (شرط نمایش)، `setFlagOnApprove`, `setFlagOnReject`.
**فیلدهای داستانی/بصری (تازه):** `orderInMonth` (ترتیبِ کارت داخلِ ماهِ داستانی)، `illustration` (Sprite اختیاری برای تصویرِ کارت/ریزکاتسین)، `approveSfx`/`rejectSfx` (AudioClip اختیاری — صدای مخصوصِ هر کارت؛ اگه خالی باشه صدای پیش‌فرضِ بله/خیر پخش می‌شه).
کارت‌ها به‌صورت asset ساخته می‌شن. **کارت‌های داستانیِ هر ماه تو `Assets/Resources/Story/<اسمِ ماه>/` هستن** (مثلاً `Resources/Story/Farvardin/`) تا خودکار با `Resources.LoadAll` لود بشن — نیازی به وایرینگِ دستی تو `allCards` ندارن.

### CardDatabase.cs — Singleton، انتخاب و مدیریت کارت‌ها
- `allCards`: لیست همه‌ی CardData (باید دستی تو Inspector پر بشه، فعلاً روش خودکار/بارگذاری از پوشه پیاده نشده — می‌تونه یه بهبود خوب باشه: بارگذاری خودکار همه‌ی CardData از یه پوشه با `Resources.LoadAll` یا Addressables)
- وزن‌های دسته‌بندی: normalWeight=60, crisisWeight=20, chainWeight=15, specialWeight=5 (قابل تنظیم)
- `GetNextCard()`: **دو حالت داره.** اگه `storyMode` روشن باشه (پیش‌فرض روشن)، کارت‌های ماهِ داستانی رو **به‌ترتیبِ `orderInMonth`** از `Resources/<storyResourcesPath>` (پیش‌فرض `Story/Farvardin`) پشت‌سرِهم برمی‌گردونه (نه تصادفی)؛ وقتی تموم شد `null` می‌ده و `StoryMonthComplete` می‌شه true. اگه `storyMode` خاموش باشه، همون انتخابِ تصادفیِ وزن‌دارِ قبلی (`GetNextRandomCard`) اجرا می‌شه (برای ماه‌های تصادفیِ آینده).
- `StartStoryMonth()`: کارت‌های ماه رو از Resources لود و مرتب می‌کنه؛ `CardSwipe.BeginGame()` صداش می‌زنه.
- `SetFlag(string)` / `HasFlag(string)`: مدیریت پرچم‌های فعال

### CardSwipe.cs — روی خود آبجکت Card
- مکانیک سوایپ با OnMouseDown/Drag/Up (نیاز به BoxCollider2D و Camera.main)
- `swipeThreshold` (۱.۵ واحد world)، بعد از رد شدن آستانه، کارت با انیمیشن (Coroutine) از صفحه خارج می‌شه و می‌چرخه
- بعد از تصمیم: اثرات روی GameStats اعمال می‌شه، پرچم مربوطه (اگه بود) ست می‌شه، `GameStats.Instance.AdvanceMonth()` صدا زده می‌شه، و وضعیت خودکار سیو می‌شه (`SaveSystem.Save`) مگر اینکه بازی تموم شده باشه که سیو پاک می‌شه
- `advisorText` و `bodyText` (نوع RTLTextMeshPro) باید تو Inspector به دو تا متن زیر Canvas داخل کارت وصل بشن
- `statBars` (آرایه‌ی ۴ تایی از StatBarUI) — موقع کشیدن کارت (`OnMouseDrag`)، بر اساس جهت کشیدن (راست=approveEffects، چپ=rejectEffects) به هر نوار می‌گه چه اثری روش میاد؛ اگه فاصله‌ی کشیدن کمتر از `hintThreshold` باشه یا رها بشه، پیش‌نمایش‌ها مخفی می‌شن
- **دیگه تو `Start()` خودکار کارت لود نمی‌کنه** — منتظر می‌مونه `BeginGame()` از `MainMenuUI` صدا زده بشه (تا وقتی منوی اصلی بسته نشده، بازی شروع نمی‌شه)
- **`FitColliderToCard()`**: BoxCollider2D کارت تو صحنه فقط ۱×۱ واحد بود ولی خودِ کارت بزرگ‌تره؛ این متد Collider رو هم‌اندازه‌ی اسپرایت کارت می‌کنه تا کل کارت سوایپ بشه.
- **`EnsureTextRendersAboveCard()`**: چون Card cover یه SpriteRenderer ماته (sortingOrder=۰) و Canvasِ متنِ کارت هم پیش‌فرض sortingOrder=۰ داشت، کارت روی متن می‌افتاد. این متد Canvasِ متن رو `overrideSorting=true` و sortingOrder بالاتر می‌کنه.
- **`ConfigureCardText()`**: متن‌های کارت تو صحنه رنگ مشکی و fontSize میکروسکوپی داشتن → نامرئی. این متد Canvasِ متن و خودِ AdvisorText/BodyText رو بر اساس اندازه‌ی واقعیِ اسپرایت کارت و مقادیر چیدمان می‌چینه.
  - **کنترلِ اینکه کد بازنویسی کنه یا نه:** فیلد `autoConfigureCardText` (پیش‌فرض روشن). روشن = موقع Start از روی مقادیر پایین چیدمان رو اعمال می‌کنه. **اگه توسعه‌دهنده بخواد خودش دستی متن رو تو صحنه تنظیم کنه، این رو خاموش می‌کنه تا Play دیگه روش ننویسه.** یه دکمه‌ی راست‌کلیکِ کامپوننت هم هست («اعمال چیدمان متن کارت روی صحنه» = `ApplyCardTextLayoutInEditor`) که تو حالت Edit مقدار اولیه‌ی درست رو یه‌بار روی صحنه می‌ذاره؛ روند پیشنهادی: یه‌بار دکمه رو بزن → `autoConfigureCardText` رو خاموش کن → دستی تنظیم کن.
  - مقادیر چیدمان (وقتی auto روشنه): `cardTextColor`, `textWidthFraction`, `advisorYFraction`/`advisorFontFraction`, `bodyYFraction`/`bodyFontFraction`/`bodyHeightFraction`.
- **`swipeRightMeansApprove`** (فیلد Inspector): تعیین می‌کنه کدوم جهتِ سوایپ «بله/تایید» باشه — روشن: راست=تایید، چپ=رد؛ خاموش: برعکس. کارت همیشه به سمتی که کشیده شد پرت می‌شه (فیزیکِ سوایپ)، ولی تایید/رد بودنش از این گزینه میاد.
- **نشانگرِ بله/خیر به سبکِ Reigns (`CreateSwipeIndicators`):** موقعِ کشیدنِ کارت، **کلِ کارت یه ته‌رنگِ سبز(بله)/قرمز(خیر) می‌گیره** (`cardTintImg` — یه Image که ناحیه‌ی داخلیِ کارت رو می‌پوشونه و به‌عنوانِ اولین فرزندِ Canvas پشتِ متن می‌شینه) و کلمه‌ی سفیدِ درشتِ «بله» **سمتِ راستِ کارت** / «خیر» **سمتِ چپِ کارت** (`cardChoiceLabel`) با شفافیتِ متناسب با فاصله ظاهر می‌شه (`UpdateSwipeIndicators`)؛ رها که کنی محو می‌شه. همه فرزندِ Canvasِ کارتن، پس با کارت می‌چرخن. (نسخه‌های قبلی — دو متنِ لبه‌ی صفحه، و بعد نوارِ مستطیلیِ توپُر — جایگزین شدن چون بد به‌نظر می‌رسیدن؛ فیلدهای `indicator*` دیگه استفاده نمی‌شن.)
- **افکت صوتیِ تصمیم (`PlayDecisionSound`):** موقعِ تصمیم، صدای مناسب پخش می‌شه — اگه کارت `approveSfx`/`rejectSfx`ِ مخصوصِ خودش رو داشته باشه اون، وگرنه صدای پیش‌فرضِ `approveClip` (`yesEffect`) / `rejectClip` (`noEffect`). یه `AudioSource` موقعِ Start با کد رو خودِ کارت اضافه می‌شه (نیازی به وایرینگ نداره)، دو کلیپِ پیش‌فرض تو Inspector وصلن. `sfxVolume` هم قابل تنظیمه.
- **حالتِ داستانی (کمپینِ فروردین):** `BeginGame()` اگه `storyMode` روشن باشه `CardDatabase.StartStoryMonth()` رو صدا می‌زنه و کارت‌ها به‌ترتیب میان. تو حالتِ داستانی **ماه با هر تصمیم جلو نمی‌ره** (هر ۱۲ کارت داخلِ همون فروردین‌ان)؛ وقتی کارت‌ها تموم شد، `EndStoryMonth()` کارنامه‌ی ماه (`MonthReportUI`) رو نشون می‌ده. باختِ عادی (شاخص=۰/۱۰۰) سرِ جاشه و همون `GameOverUI` رو می‌آره.
- **انیمیشنِ ظاهرشدن (`AppearAnimation`):** هر کارتِ جدید با یه پرشِ ریزِ اندازه (۸۵٪→۱۰۰٪) ظاهر می‌شه تا گیم‌پلی خشک نباشه.

### MonthReportUI.cs — کارنامه‌ی پایانِ ماهِ داستانی (روی «روزنامه»)
- **پس‌زمینه = تصویرِ روزنامه** (`Assets/Resources/UI/Newspaper.png`) که وسطِ صفحه با نسبتِ درست چیده می‌شه و متن‌ها (ماستهد «کارنامه‌ی فروردین» تو بنرِ مشکی، اسمِ پایان، متنِ طنز، تیترِ روزنامه، و اعدادِ نهایی) روی کاغذِ کاهیش با رنگِ قهوه‌ایِ تیره نوشته می‌شن. یه **دکمه‌ی خانه (`UI/Btn_Home`)** پایینش هست که برمی‌گرده به منوی اصلی.
- موقعِ اجرا با کد ساخته می‌شه (`MonthReportUI.Create(canvas, font)` که `CardSwipe.BeginGame` صداش می‌زنه) — **نیازی به وایرینگ تو صحنه نداره**، مثل نشانگرهای بله/خیر.
- `Show(budget, popularity, security, diplomacy)`: بر اساسِ وضعیتِ نهایی یکی از **۴ پایان** رو انتخاب می‌کنه (مقایسه‌ی محبوبیت با میانگینِ بودجه/امنیت/دیپلماسی حول عددِ ۵۰):
  - محبوبیت↑ و کفایت↑ → «قهرمانِ ملی»
  - محبوبیت↑ و کفایت↓ → «پوپولیستِ محبوب»
  - محبوبیت↓ و کفایت↑ → «تکنوکراتِ بی‌رأی»
  - هردو↓ → «کاندیدای فراموش‌شده»
- هر پایان: عنوان + متنِ طنز + یه «تیترِ روزنامه» + خطِ اعدادِ نهایی + **خطِ ریالِ به‌دست‌آمده/موجودی**. متن‌ها بزرگ‌تر شدن.
- **دو دکمه‌ی تصویریِ پایین (`AddImageButton`):** «بازیِ دوباره» (`UI/Btn_Replay`، از `Art/Replay`) و «خانه» (`UI/Btn_Home`) — هر کدوم با نسبتِ تصویرِ خودش (کامل و بدونِ بریدگی)، وسطِ نیمه‌ی چپ/راست.
- **عکسِ اختیاریِ پایان:** اگه فایلی به اسمِ `Resources/Story/Farvardin/Endings/ending_0..3` بذاری، بالای کارنامه نشون داده می‌شه (فعلاً خالیه — جای عکس آماده‌ست).

### StatBarUI.cs — روی هر آبجکتِ نوار وضعیت (طرحِ «بج»)
**طرحِ جدید (بازنویسی شد):** به‌جای اسلایدرِ چرخیده‌ی قدیمی، حالا هر شاخص یه **بجِ آماده** (تصویرِ کاملِ نوار با آیکون و اسمِ پخته‌شده داخلش) داره که موقعِ اجرا با کد بالای صفحه، صاف و مرتب، روی Canvas ساخته می‌شه. اسکریپت فقط **پُرشدنِ طلایی + عددِ شاخص + متنِ +/-** رو مدیریت می‌کنه.
- تصاویرِ بج تو **`Assets/Resources/Bars/`** هستن (`Bar_Budget/Bar_Popularity/Bar_Security/Bar_Diplomacy.png`) و با `Resources.Load<Sprite>("Bars/...")` بارگذاری می‌شن — پس **وایرینگِ دستی تو صحنه لازم نیست**. (این ۴ تصویر از برشِ خودِ عکسِ طرحی که توسعه‌دهنده فرستاد ساخته شدن.)
- `HideOldSliderVisual()`: ویژوالِ اسلایدرِ چرخیده‌ی قدیمی (Background/Fill Area/Handle) رو مخفی می‌کنه تا فقط بجِ جدید دیده بشه. (فیلدهای قدیمیِ اسم/آیکون/چرخش حذف شدن.)
- `BuildBadge()`: بج رو با anchor به بالای Canvas و در **اسلاتِ افقیِ خودش** می‌ذاره (چیدمانِ چپ→راست: دیپلماسی، بودجه، محبوبیت، امنیت؛ مساوی و بدونِ روی‌هم‌افتادگی — `SlotXFraction`). ارتفاع از `barHeight` و عرض خودکار از نسبتِ تصویر.
- **Fill Area:** پنلِ پایینیِ بج با کسرهای `fillLeftFrac`/`fillRightFrac`/`fillBottomFrac`/`fillTopFrac` (نسبت به خودِ تصویرِ بج) تعریف می‌شه؛ داخلش یه Imageِ طلایی (`fillColor`) هست که `anchorMax.y`ش با مقدارِ شاخص (۰..۱۰۰) از پایین به بالا پر می‌شه (`SetFill`). این کسرها تو Inspector قابلِ تنظیمن اگه Fill دقیقاً رو پنل نیفتاد.
- **عدد شاخص:** وسطِ پنل، با رقمِ فارسی (`ToPersian`)، سفیدِ bold با outline.
- **متنِ +/- (بعد از تصمیم):** `ShowHint(amount)`/`HideHint()` که CardSwipe صداشون می‌زنه، یه متنِ سبز/قرمز بالای پنل نشون می‌ده (طبقِ خواسته‌ی «اثر بعد از انتخاب»).
- **مخفی تا شروعِ بازی (رفعِ باگ):** بج‌ها موقعِ ساخت **غیرفعال** می‌مونن و فقط تو `CardSwipe.BeginGame()` (با `SetVisible(true)`) روشن می‌شن — چون قبلاً تو منوی اصلی دیده می‌شدن (بج بعد از پنلِ منو ساخته می‌شد و روش می‌افتاد). پنل‌های تمام‌صفحه هم موقعِ نمایش `SetAsLastSibling()` می‌شن.
- **تصاویرِ بج از `Sagsa.png`:** طرحِ نهایی (`Assets/Art/Sagsa.png`، ۱۷۷۴×۸۸۷) که پنلِ پایینیِ **مستطیلی** داره (نه نوک‌تیز، تا Fill از کادر نزنه بیرون) به ۴ بج برش خورد و تو `Resources/Bars/` جایگزین شد. کسرهای Fill از روی خودِ تصویر اندازه‌گیری شدن تا **کلِ باکس** رو پر کنن (پیش‌فرض: L=0.125, R=0.85, B=0.03, T=0.44 — باکسِ واقعی x[0.122,0.849]). عددِ شاخص **مشکی با دورخطِ کرمِ روشن** تا رو طلایی و رو تیره خوب دیده بشه.
- **ماهِ فصلِ داستانی:** `CardDatabase.storyMonthNumber` (فروردین=۱) و `GameStats.SetMonth(...)` که تو `CardSwipe.BeginGame` صدا زده می‌شه، تضمین می‌کنه موقعِ کمپین تاریخ همیشه «سال اول — فروردین» باشه (حتی اگه سیوِ قدیمی ماهِ دیگه‌ای داشته باشه). فونتِ باکسِ تاریخ هم از ۳۴ به ۴۸ رفت تا واضح‌تر باشه.
- **جای کارت:** آبجکتِ Card تو صحنه پایین‌تر اومد (y از ~۰ به -۱.۳) تا با نوارهای بالای صفحه تداخل نکنه.

### GameOverUI.cs — صفحه‌ی پایان بازی (باخت/پیروزی)
- رو `GameManager` نصب شده، سه فیلد داره: `Persian Font`، `Target Canvas`، و `Card Swipe` (برای دکمه‌ی تبلیغ)
- کل UI (پس‌زمینه‌ی تیره + متن پیام + دو دکمه) رو تو `BuildUI()` با کد می‌سازه، نه از پیش تو صحنه — چون امکان ساختن دستیش تو Editor نبود
- به `GameStats.OnGameLost` و `OnGameWon` گوش می‌ده؛ موقع باخت بر اساس اینکه کدوم شاخص و به کدوم مرز (۰ یا ۱۰۰) رسیده، یکی از ۸ پیام `GetLossMessage()` رو نشون می‌ده (این پیام‌ها فعلاً ساده‌ن، موقع نوشتن محتوای اصلی باید بهتر نوشته بشن)
- متن‌ها از نوع `RTLTextMeshPro` هستن (نه TMP معمولی) چون فارسی‌ان
- **دو دکمه موقع باخت:**
  - «دیدن تبلیغ و ادامه» — فقط اگه هنوز تو این بازی استفاده نشده باشه نشون داده می‌شه؛ می‌زنه `GameStats.RecoverStatViaAd()` (شاخص بحرانی رو به ۳۰ برمی‌گردونه، طبق سند طراحی) و بازی رو با `CardSwipe.BeginGame()` ادامه می‌ده. **فعلاً هیچ SDK تبلیغ واقعی وصل نشده** — این فقط شبیه‌سازیه، بعداً باید با تپسل/ادیوری واقعی جایگزین بشه.
  - «بازگشت به منو» — صحنه رو دوباره لود می‌کنه که خودکار می‌ره سراغ منوی اصلی (چون CardSwipe دیگه خودکار شروع نمی‌شه)
- موقع پیروزی فقط دکمه‌ی «بازگشت به منو» نشون داده می‌شه (دکمه‌ی تبلیغ معنی نداره)
- `backButtonSprite` (اختیاری) — اگه وصل بشه، دکمه‌ی «بازگشت به منو» به‌جای رنگ ساده از این عکس استفاده می‌کنه (الان وصله به «بازگشت» از ButtonAssets)

### MainMenuUI.cs — منوی اصلی
- رو `GameManager` نصب شده، سه فیلد اصلی داره: `Persian Font`، `Target Canvas`، و `Card Swipe` (رفرنس به اسکریپت CardSwipe رو آبجکت Card)
- مثل GameOverUI کل UI رو با کد می‌سازه. **چیدمانِ جدید (طبق طرحِ توسعه‌دهنده):** پس‌زمینه‌ی دفترِ ریاست‌جمهوری (`BG`) + سه دکمه‌ی وسط‌چین از بالا به پایین: **«شروع بازی»**, **«تنظیمات»**, **«خروج»**. (دکمه‌ی جداگانه‌ی «ادامه» حذف شد.)
- فیلدهای تصویری: `backgroundSprite` (BG)، `startButtonSprite` (اسلایسِ «شروع بازی» از ButtonAssets)، `settingsButtonSprite` (`Settings.png`)، `exitButtonSprite` (`Exit.png`). اگه هر کدوم خالی بمونه، به‌جاش دکمه‌ی رنگ‌ساده‌ی متنی ساخته می‌شه.
- **چیدمان (`AddMenuButton`):** هر دکمه پهنای ثابت (`buttonWidthFraction`، پیش‌فرض ۰.۴۴) داره و **ارتفاعش خودکار از نسبتِ خودِ تصویر** حساب می‌شه (با `preserveAspect`) تا دکمه کش نیاد؛ جای هر دکمه با `startButtonCenter`/`settingsButtonCenter`/`exitButtonCenter` (کسری از صفحه) تنظیم می‌شه.
- **رفتارِ دکمه‌ها:**
  - «شروع بازی» (`StartGame`): **پنلِ انتخاب** (`StartPanel`) رو باز می‌کنه که دو دکمه داره: «شروع بازی جدید» و «ادامه بازی قبلی». (نه مستقیم بازی شروع می‌شه.)
  - «تنظیمات» (`OpenSettings`): صفحه‌ی تنظیمات (`SettingsUI.Open`) رو باز می‌کنه — بخشِ بعدی رو ببین.
  - «خروج» (`QuitGame`): `Application.Quit()` (و تو Editor، توقفِ Play).
- **پنلِ شروع/ادامه (`BuildStartPanel`):** موقعِ اجرا با کد ساخته می‌شه (مثلِ بقیه‌ی UI)، تصاویرش از `Resources/UI/` لود می‌شن (`StartBox` = باکسِ تزئینیِ وسط، `Btn_NewGame` = «شروع بازی جدید»، `Btn_Continue` = «ادامه بازی قبلی») — پس **نیازی به وایرینگِ صحنه نیست**. این سه تصویر از برشِ عکسِ «Start» که توسعه‌دهنده فرستاد ساخته شدن. دو دکمه فرزندِ باکس‌ان (بالا=جدید، پایین=ادامه). کلیک روی فضای خالیِ تیره پنل رو می‌بنده (`HideStartPanel`).
  - «شروع بازی جدید» (`StartNewGame`): سیو رو پاک، شاخص‌ها/پرچم‌ها رو ریست، و بازی رو از فروردین شروع می‌کنه.
  - «ادامه بازی قبلی» (`ContinueGame`): سیوِ ذخیره‌شده رو لود می‌کنه (شاخص‌ها + پرچم‌ها + `storyIndex` = همون کارتی که بود) و ادامه می‌ده. **اگه سیوی نباشه این دکمه کم‌رنگ و غیرفعاله** (`SetContinueEnabled`).
- **نکته‌ی import:** `Settings.png` و `Exit.png` اولش spriteMode=Multiple بودن (خودکار اسلایس شده بودن)؛ تو meta به **spriteMode=Single** تغییر داده شدن تا کلِ تصویر یه اسپرایتِ واحد (fileID 21300000) باشه و به‌عنوان یه دکمه استفاده بشه.

### SaveSystem.cs / SaveData.cs — ذخیره‌سازی
- `SaveData`: یه کلاس ساده (Serializable) با ۴ شاخص، ماه فعلی، و لیست پرچم‌های فعال
- `SaveSystem`: کلاس static با `Save()`/`Load()`/`HasSave()`/`DeleteSave()` — با `JsonUtility` رو یه فایل JSON تو `Application.persistentDataPath` ذخیره می‌کنه
- **محدودیت فعلی:** تاریخچه‌ی ۵ کارت اخیر (برای جلوگیری از تکرار سریع) تو سیو ذخیره نمی‌شه — بعد از «ادامه‌ی بازی» این تاریخچه خالیه. مشکل مهمی نیست، فقط یعنی بلافاصله بعد از ادامه دادن ممکنه یه کارت تکراری زودتر از حد معمول بیاد.

### RialSystem.cs — ارزِ بازی «ریال» (جایگزینِ امتیاز)
- ارزِ ماندگارِ بازی. ارزشش بالاست و به‌دست‌آوردنش سخته (هر دور فقط چند ریال). تو `PlayerPrefs` (`DolatShow_Rial`) جمع می‌شه.
- `Total` موجودی، `Add(amount)` اضافه‌کردن، `EarnFromStats(b,p,s,d)` = ریالِ یه دور (فقط بابتِ بالای میانگین نگه‌داشتنِ کشور، `(مجموع-۲۰۰)/۲۵` ≈ ۰..۸)، `RewardRun(...)` حساب+اضافه می‌کنه و مقدار رو برمی‌گردونه. `TotalPersian()` رقمِ فارسی.
- **جایگزینِ امتیاز شد:** `MonthReportUI` و `GameOverUI` به‌جای «امتیاز/رکورد»، «ریالِ به‌دست‌آمده + موجودی» نشون می‌دن. `MainMenuUI` موجودی رو تو **باکسِ ریال** (بالا-چپ) نشون می‌ده (`BuildRialBox`، تصویر `Resources/UI/RialBox` که از `Art/Rial Box` کپی شد؛ عدد داخلِ کادرِ راستِ تصویر، شروع از ۰).
- `ScoreSystem.cs` هنوز تو پروژه هست ولی **دیگه استفاده نمی‌شه** (جاش رو ریال گرفت).
- **باکسِ پروفایلِ منو (`MainMenuUI.BuildProfileBox`)**: تصویر `Resources/UI/ProfileBox.png` (۱۹۶۲×۸۰۲؛ بدونِ فاصله تو اسم — فاصله تو اسمِ فایل باعثِ مشکلِ لود می‌شد). چیدمان: **دایره‌ی چپ = آواتار**، **بنرِ بالا (وسط) = اسمِ کاربر**، **نشانِ «سطح» = عددِ سطح** (فقط رقم، چون کلمه‌ش تو نشانه)، **بنرِ پایین = ریال**. کسرهای چیدمان تو کد قابلِ تنظیمن. پنلِ پروفایل و تنظیمات هم از `UI/Box.png` (باکسِ عمودیِ تزئینی) استفاده می‌کنن. (⚠️ متاهای این دو عکس اولش spriteMode=Multiple بودن → به Single بازسازی شدن وگرنه `Resources.Load<Sprite>` null می‌داد.)

### PauseUI.cs — سیستمِ توقف موقعِ گیم‌پلی (تازه)
- یه **دکمه‌ی گِردِ توقف** گوشه‌ی بالا-چپ (فقط موقعِ گیم‌پلی، با رویدادِ `CardSwipe.GameStarted` روشن می‌شه — مثلِ نوارهای وضعیت). کلیک روش یه **باکس** (`UI/Box`) باز می‌کنه با سه دکمه: **ادامه بازی** (Resume)، **بازی دوباره** (`CardSwipe.RestartNewGame`)، **خانه** (ری‌لودِ صحنه → منو).
- موقعِ باز بودنِ پنل، `cardSwipe.enabled=false` می‌شه تا سوایپِ کارت متوقف بمونه؛ Resume دوباره `true`ش می‌کنه. کلیک روی فضای تیره‌ی پنل هم = ادامه.
- `MainMenuUI.Start` با `PauseUI.Create(canvas, font, cardSwipe)` می‌سازتش.
- **تصویرها:** از اطلسِ برش‌خورده‌ی دکمه‌های توقف تو `Resources/UI` با `Resources.LoadAll<Sprite>` لود می‌شن و با کلیدواژه‌ی نامِ برش پیدا می‌شن (`continu/resume`→ادامه، `home`→خانه، `replay`→دوباره، `step/stop/pause`→دکمه‌ی توقف). اسمِ فایلِ اطلس از چند کاندیدا امتحان می‌شه (`Step`, `Steppe`, `Stop`, `Pause`, ...). **این اطلس باید spriteMode=Multiple بمونه** (برخلافِ بقیه‌ی عکس‌ها) چون چند اسپرایتِ نام‌دار داخلشه. اگه اطلس پیدا نشد، دکمه‌های متنیِ موقت ساخته می‌شن.

### StoreSystem.cs / StoreUI.cs — فروشگاه (تازه)
- **`StoreSystem`** (static، داده‌محور): کاتالوگِ آیتم‌ها (`Catalog`) + خرید با ریال (`Buy` → `RialSystem.Spend`) + مالکیت/تعداد تو `PlayerPrefs` (`DolatShow_Store_<id>`). برای آیتمِ جدید فقط یه سطر به `Catalog` اضافه کن. دو نوع: `Consumable` (شمارنده‌دار، `GetCount`/`Consume`) و `Permanent` (یه‌بار، `IsOwned`).
  - **آیتم‌های فعلی:** `headstart` (شروع +۱۰ همه شاخص‌ها، ۶ ریال)، `superstart` (+۲۰، ۱۲ ریال)، `veto` (نجاتِ بدونِ تبلیغ موقعِ باخت، ۱۰ ریال) — همه Consumable؛ و `frame_royal` (قابِ سلطنتی) + `theme_dark` (تمِ کارت) — Permanent (اثرِ بصری‌شون فعلاً پیاده نشده، جای آیکون/تم بعداً).
  - **اثرها که وصل شدن:** `ConsumeStartBonus()` قوی‌ترین شروع رو مصرف می‌کنه؛ `MainMenuUI.ApplyStoreStartBonus()` تو شروعِ بازیِ جدید (قبلِ `BeginGame`) صداش می‌زنه و با `ApplyEffect` روی ۴ شاخص اعمالش می‌کنه. `veto` تو `GameOverUI.ShowVetoButton/UseVeto` — دکمه‌ی «استفاده از وتو» موقعِ باخت (اگه موجودی داشته باشه) بدونِ تبلیغ کشور رو نجات می‌ده.
- **`StoreUI`** (کد‌محور): پس‌زمینه `Resources/UI/Store background.png` (تمام‌صفحه) + موجودیِ ریال + لیستِ آیتم‌ها (اسم/توضیح/دکمه‌ی خرید با قیمت) + پیامِ خرید + دکمه‌ی بستن. با خرید، لیست و موجودی رفرش می‌شن. `MainMenuUI.OpenStore()` (دکمه‌ی «فروشگاه» تو منوی کشویی) بازش می‌کنه. **UIِ نهایی/آیکونِ آیتم‌ها بعداً کامل می‌شه.** (⚠️ متای `Store background.png` اولش Multiple بود → Single شد.)

### SettingsSystem.cs / SettingsUI.cs / MusicManager.cs — صفحه‌ی تنظیمات (تازه)
- **`SettingsSystem`** (static): حالتِ تنظیمات تو `PlayerPrefs` (`DolatShow_SoundOn`, `DolatShow_SoundVol`, `DolatShow_MusicOn`, `DolatShow_MusicVol`, `DolatShow_Vibration`). گزینه‌ها: **صدا** (روشن/خاموش + بلندی)، **موسیقی** (روشن/خاموش + بلندی)، **لرزش**.
  - **صدا چطور کنترل می‌شه؟** با `AudioListener.volume` — یه ضریبِ *سراسری* روی همه‌ی AudioSourceهای بازی (کارت، دیالوگ، تشویق، شمارش)، پس بدونِ دستکاریِ تک‌تکِ فایل‌ها کلِ صدا کنترل می‌شه. `Apply()` باید موقعِ شروع صدا زده بشه (تو `MainMenuUI.Start`).
  - `Vibrate()`: لرزشِ کوتاه (`Handheld.Vibrate`، فقط روی اندروید/بیلد نه ادیتور) اگه لرزش روشن باشه — `CardSwipe.PlayDecisionSound` بعد از هر تصمیم صداش می‌زنه.
- **`MusicManager`** (MonoBehaviour، `DontDestroyOnLoad`): آهنگِ اصلیِ بازی (`Resources/Sound/OrgenalSund.mp3`) رو **فقط تو منوی اصلی** به‌صورتِ لوپ پخش می‌کنه؛ **موقعِ شروعِ بازی (`CardSwipe.GameStarted`) قطع می‌شه** و با برگشت به منو (ری‌لودِ صحنه → `MainMenuUI.Start`) دوباره از اول پخش می‌شه. کنترلِ «تو منو هستیم یا نه» با `SetMenu(bool)`ه: `MainMenuUI.Start` → `SetMenu(true)`، و رویدادِ `GameStarted` → `SetMenu(false)`. **هر جا خانم مشاور حرف می‌زنه هم آهنگ قطع می‌شه:** `TutorialUI.Show` و `ResultGreetingUI.Show` هم `SetMenu(false)` می‌زنن (و `ResultGreetingUI.Finish` چون تو منوئه دوباره `SetMenu(true)` می‌زنه). از تنظیماتِ موسیقی (روشن/خاموش + بلندی) هم پیروی می‌کنه (`ApplyToSource`: `inMenu && MusicOn && clip!=null`). `MainMenuUI.Start` با `EnsureExists()` می‌سازتش. اگه فایلِ آهنگ نباشه بی‌صدا و بی‌خطا کنار می‌مونه.
- **`SettingsUI`** (کد‌محور، مثلِ بقیه‌ی UI): پنلِ تیره + باکسِ تزئینیِ عمودی (`UI/Box`) + سوییچ‌های روشن/خاموش (سبز/قهوه‌ای، دستگیره‌ی کشویی) + دو نوارِ بلندی (`Slider` با fill طلایی) + **دکمه‌ی «پاک‌کردنِ اطلاعات»** (با تاییدِ دومرحله‌ای → `SaveSystem.DeleteSave()` + `PlayerPrefs.DeleteAll()` + ری‌لودِ صحنه) + نمایشِ نسخه (`Application.version`). `MainMenuUI.OpenSettings()` با `SettingsUI.Open(canvas, font)` بازش می‌کنه.

### UISound.cs — صدای کلیکِ سراسریِ دکمه‌ها (تازه)
- به‌جای وصل‌کردنِ دستیِ صدا به تک‌تکِ ۷۰+ دکمه‌ی بازی، این سیستمِ **سراسری** خودکار همه‌ی `Button`های صحنه رو پیدا می‌کنه و بهشون صدای کلیک وصل می‌کنه — حتی دکمه‌هایی که بعداً تو پنل‌های داینامیک (تنظیمات/پروفایل/…) ساخته می‌شن.
- **چطور؟** `UISound` (MonoBehaviour، `DontDestroyOnLoad`) هر ۰.۳ ثانیه با `FindObjectsByType<Button>(FindObjectsInactive.Include, ...)` دکمه‌ها رو اسکن می‌کنه؛ هر دکمه‌ای که هنوز وصل نشده یه نشانگرِ `UISoundHooked` می‌گیره و `onClick`ش صدای کلیک می‌زنه (تا دوبار وصل نشه). `MainMenuUI.Start` با `EnsureExists()` می‌سازتش و بعد از `BuildUI` یه‌بار `HookAll()` می‌زنه (بی‌تأخیر برای دکمه‌های منو).
- **صدا:** `Assets/Resources/Sound/Click.wav` (فعلاً یه تیکِ نرمِ ساخته‌شده با کد — non-LFS). **می‌تونی این فایل رو با هر کلیکِ دلخواهِ خودت جایگزین کنی** (همون اسم `Click.wav` تو همون پوشه). چون از AudioSource پخش می‌شه، خودکار از تنظیماتِ صدا (`AudioListener.volume` → گزینه‌ی «صدا» تو تنظیمات) پیروی می‌کنه.

### پکِ «Juice» و اعتیادآوری (بازخوردِ حسی — تازه اضافه شد)
بر اساسِ تحقیق روی Reigns و اصولِ Game Feel، این‌ها اضافه شدن (همه کد‌محض، بدونِ عکسِ جدید):
- **`StatBarUI`:** موقعِ تغییرِ شاخص، عدد و نوار با انیمیشنِ نرم (`AnimateChange`, `animDuration`) به مقدارِ جدید **پرش/count-up** می‌کنن؛ بج یه **پرشِ اندازه** (`popScale`) و عدد یه **فلشِ رنگ** (سبز↑/قرمز↓) می‌خوره. وقتی شاخصی به لبه‌ی باخت نزدیکه (≤`dangerThreshold` یا ≥۱۰۰منهای اون)، یه **لایه‌ی قرمز روی بج نبض می‌زنه** (`Update` + `dangerOverlay`) تا حسِ خطر بسازه.
- **`CardSwipe`:** موقعِ تصمیم **صفحه می‌لرزه** (`ShakeCamera`/`TriggerShake`) — شدتش با بزرگیِ اثر و موقعِ باخت بیشتره. + `RestartNewGame()` برای شروعِ فوریِ بازیِ جدید.
- **دکمه‌ی «بازیِ دوباره»** تو `MonthReportUI` و `GameOverUI` که فوری (بدونِ برگشت به منو) بازیِ جدید شروع می‌کنه — حلقه‌ی سریعِ «یه بار دیگه».

### TutorialUI.cs — بخشِ آموزشیِ اولِ بازی (دیالوگ + صدای خانم رستمی)
- موقعِ **بازیِ جدید** (نه ادامه/تبلیغ‌وادامه) از `MainMenuUI.StartNewGame` صدا زده می‌شه: `TutorialUI.Show(canvas, font, onComplete)`. وقتی دیالوگ‌ها تموم شد `onComplete` → `CardSwipe.BeginGame()`.
- کاملاً با کد ساخته می‌شه؛ همه‌چیز از Resources لود می‌شه (بدونِ وایرینگِ صحنه):
  - کاراکتر: **`Resources/Characters/Advisor.png`** (پایین-چپ)
  - باکسِ دیالوگ: **`Resources/UI/DialogBox.png`** (بالا؛ عمداً کمی کشیده/بزرگ‌تر شده — `boxHeightMultiplier`/`boxWidthFrac` — تا متن جا شه؛ متن هم autosize داره)
  - دکمه‌ی رد کردن: **`Resources/UI/Reject.png`** (گوشه‌ی بالا-چپ؛ اگه نبود، دکمه‌ی متنی)
  - **صدای هر خط:** `Resources/Voice/Dialogue/Dialog_1..Dialog_7` (WAV، non-LFS). با هر صفحه، صدای همون خط پخش می‌شه (`AudioSource` رو خودِ آبجکت). **باکسِ دیالوگ عمداً کشیده/بزرگ شده (`boxHeightMultiplier`) و متن autosize با کف پایین داره تا سرریز نکنه.**
- قابِ داستان: بازیکن **کاندیدای** انتخاباته و **فروردین ماهِ کمپینه** (هنوز رئیس‌جمهور نشده). لحنِ مشاور: آرام و حرفه‌ای، بدونِ کنایه. **متنِ روی صفحه باید دقیقاً با فایل‌های صوتی یکی باشه** (آرایه‌ی `lines`؛ اگه متنی عوض شد، صداش هم باید از نو ساخته شه).
- دیالوگ‌ها (۷ خط) با ضربه به صفحه جلو می‌رن؛ دکمه‌ی رد کردن هم هست. اسمِ مشاور = `advisorName` = «خانم رستمی».
- **مهم برای صدا:** فایل‌های صوتی که توسعه‌دهنده تو `Assets/Sound/Dialogue/` گذاشته بود به `Assets/Resources/Voice/Dialogue/` منتقل شدن (چون `Resources.Load` فقط از پوشه‌ی Resources کار می‌کنه) و به‌صورتِ non-LFS ذخیره شدن (LFS تو محیطِ توسعه برای پوش بلاکه). اگه صدای جدید ساختی، مستقیم تو همون مسیرِ Resources بذار.

### RuntimeUIHelper.cs — کمکی مشترک
- چون GameOverUI، MainMenuUI، YearDisplayUI، و BackgroundUI هر کدوم بخشی از UI‌شون رو با کد می‌سازن (نه از پیش تو صحنه)، این کلاس static متدهای مشترک رو بینشون به اشتراک می‌ذاره تا کد تکراری نباشه:
  - `CreateFullScreenPanel` / `CreateButton` — `CreateButton` حالا خودکار از قابِ تزئینیِ `Resources/UI/Button.png` به‌عنوانِ پس‌زمینه‌ی همه‌ی دکمه‌های متنی استفاده می‌کنه (متن کرمِ روشن، autosize)؛ اگه اون تصویر نبود به رنگِ ساده برمی‌گرده. (سوییچ‌های تنظیمات جدان و این رو نمی‌گیرن.)
  - `CreateRTLText` — متن فارسی
  - `CreateImage` — یه Image ساده از یه Sprite (پس‌زمینه، لوگو، آیکون) — `stretch:true` یعنی دقیقاً پر کن (پس‌زمینه‌ها)، `false` یعنی نسبت تصویر حفظ بشه (لوگو/آیکون)
  - `CreateImageButton` — دکمه‌ای که خودِ عکسش برچسب/متن داره (مثل دکمه‌های آماده‌ی «شروع بازی»/«بازگشت»)

### سیستمِ Prefab برای صفحه‌های UI (تازه — مهم)
توسعه‌دهنده خواست صفحه‌های چیدمانی به‌جای ساختِ کامل با کد، به **Prefabِ قابلِ ویرایش تو ادیتور** تبدیل بشن (تا با موس تنظیمشون کنه، بدونِ منتظرِ کد موندن). الگوی هیبریدی که پیاده شد:
- Prefabها تو `Assets/Resources/Prefabs/` هستن: **`MainMenu`، `StartPanel`، `DateBox`، `GameOverPanel`، `TutorialPanel`**. (نوارهای شاخص `StatBarUI` و کارنامه `MonthReportUI` عمداً تو کد موندن چون داینامیک/انیمیشن‌محورن.)
- هر اسکریپتِ UI اول با `Resources.Load<GameObject>("Prefabs/...")` امتحان می‌کنه؛ اگه Prefab بود `Instantiate` می‌کنه و فقط **رفتار** (فونتِ فارسی، متنِ داینامیک، کلیکِ دکمه‌ها، صدا) رو با کد وصل می‌کنه. اگه Prefab نبود، به **روشِ کدیِ قبلی برمی‌گرده** (متد `BuildFromPrefab()`/`BuildStartPanelFromPrefab()` که `false` می‌ده) — پس هیچ صفحه‌ای خراب نمی‌شه.
- کلیکِ دکمه‌ها تو Prefab به متد وصل نمی‌شه؛ کد بعد از Instantiate با اسمِ فرزند پیداشون می‌کنه (`FindDeep`) و `onClick` رو اضافه می‌کنه. **تورِ ایمنی:** اگه کامپوننتِ `Button` از Prefab لود نشه، کد با `AddComponent<Button>` می‌سازتش.
- **شناسه‌های ثابتِ لازم برای ساختِ دستیِ Prefab** (از صحنه‌ی همین پروژه استخراج شدن): `Image` guid `fe87c0e1cc204ed48ad3b37840f39efc`؛ `Button` guid `4e29b1a8efbd4b44bb3f3716e73f07ff`؛ `RTLTextMeshPro` guid `f4688fdb7df04437aeb418b961361dc5`؛ فونتِ فارسی NotoNaskhArabic guid `1348f526aa2f6d847a6c02795af47876` با material fileID `-393344135853397389`. (⚠️ فونتِ `8f586378...` در واقع LiberationSansِ انگلیسیه — رقمِ فارسی نداره؛ استفاده نشه.)
- ژنراتورِ کمکیِ Prefab (برای صفحه‌های پرمتن) تو scratchpad ساخته شد؛ اگه Prefabِ جدید خواستی، همون الگو رو دنبال کن. **ویرایشِ ظاهر:** دابل‌کلیک رو Prefab تو Project → تنظیم با موس → Ctrl+S.

### YearDisplayUI.cs — نمایش سال و ماه فعلی (توی باکس پایینِ صفحه)
- رو `GameManager` نصب شده. یه باکس (تصویر `History.png` → فیلد `boxSprite`) پایین-وسطِ صفحه می‌سازه و متنِ «سال اول — فروردین» رو وسطش نشون می‌ده (قبلاً متنِ ساده بالای صفحه بود که رو نوارها می‌افتاد).
- فیلدهای قابل‌تنظیم: `bottomMargin` (فاصله از پایین)، `boxSize`، `fontSize`، `textColor`، `persianFont`، `targetCanvas`.
- ماه داخل سال = `(CurrentMonth-1) % 12`، به `GameStats.OnMonthChanged` گوش می‌ده و ماه‌به‌ماه آپدیت می‌شه.
- **باکسِ تاریخ تو منوی اصلی دیده نمی‌شه:** موقعِ ساخت `SetActive(false)` می‌شه و فقط وقتی بازی واقعاً شروع شد (رویدادِ استاتیکِ `CardSwipe.GameStarted` که تو `BeginGame` صدا زده می‌شه) روشن می‌شه — مثلِ نوارهای وضعیت.

### BackgroundUI.cs — پس‌زمینه‌ی ثابت گیم‌پلی
- رو `GameManager` نصب شده، فیلد اصلیش `Background Sprite` (وصله به BGGame) + `sortingOrder` (پیش‌فرض ۱۰۰-)
- **نکته‌ی معماری مهم:** کارت یه SpriteRenderer توی دنیاست، ولی Canvas اصلی از نوع Screen Space - Overlay هست که *همیشه* روی همه‌ی آبجکت‌های دنیا رندر می‌شه. برای همین نمی‌شه پس‌زمینه رو به‌صورت UI Image روی Canvas گذاشت — کارت رو کامل می‌پوشونه (این دقیقاً باگی بود که «کارت نمایش داده نمیشه» رو می‌ساخت). راه‌حل: پس‌زمینه هم یه SpriteRenderer توی دنیاست با `sortingOrder = -100` (پشت کارت که ۰ هست)، و با کد به اندازه‌ی دید دوربین (orthographic) اسکیل می‌شه.
- فیلد `targetCanvas` تو صحنه هنوز وایره ولی دیگه استفاده نمی‌شه (بی‌ضرره)

---

## ۴. وضعیت فعلی پروژه (چی کار شده، چی نشده)

```
☑ ۱. سیستم شاخص‌ها — کامل و تست‌شده (GameStats.cs, StatBarUI.cs)
☑ ۲. دیتابیس کارت‌ها — کلاس‌ها ساخته شده و تست شده (CardData/CardDatabase/CardSwipe)
☑ متن فارسی روی کارت — حل شده (RTLTMPro + NotoNaskhArabic font asset)
☑ شمارنده‌ی ماه و منطق ساده‌ی پایان بازی — پیاده‌سازی و تست شد (GameStats: CurrentMonth/AdvanceMonth/IsGameOver/OnMonthChanged/OnGameWon/OnGameLost؛ CardSwipe بعد از هر تصمیم AdvanceMonth رو صدا می‌زنه و با IsGameOver کارت جدید نمی‌ذاره)
☑ نقطه‌ی پیش‌نمایش اثر تصمیم موقع کشیدن کارت (Indicator Hint) — کامل، هم کد و هم وایرینگ تو صحنه (یه HintText غیرفعال زیر هر نوار وضعیت ساخته و به StatBarUI و به آرایه‌ی statBars تو CardSwipe وصل شده — مستقیم تو فایل SampleScene.unity انجام شد، نه دستی تو Inspector)
◐ نوشتن محتوای واقعی کارت‌ها — **فروردین (سال اول): ۱۲ کارتِ کمپینِ انتخابات نوشته و ساخته شد** (`Resources/Story/Farvardin/`، طنزِ سیاسیِ نیش‌دار، درباره‌ی مشکلاتِ واقعیِ ایران) + سیستمِ ماهِ داستانی + ۴ پایان + کارنامه‌ی ماه. **هنوز تو Editor تست نشده.** ماه‌های اردیبهشت به بعد هنوز نوشته نشدن. (۲ کارتِ تستیِ قدیمی تو `Assets/Cards` هنوز هستن ولی تو حالتِ داستانی استفاده نمی‌شن.)
☑ صفحات پایان بازی (۸ نوع باخت + صفحه‌ی پیروزی) — کد (GameOverUI.cs) و وایرینگ تو صحنه کامل شد و تست شد
☐ صفحات سالانه (گزارش هر ۱۲ ماه، تست نیمه‌راه ماه ۲۴)
☐ درآمدزایی (اتصال تپسل یا ادیوری، تبلیغ جایزه‌دار/بینابینی/بنر)
☑ منوی اصلی و ذخیره‌سازی — کد (MainMenuUI.cs, SaveSystem.cs, SaveData.cs) و وایرینگ تو صحنه کامل شد و تست شد. لیدربورد هنوز نیست.
☑ نمایش سال جاری (سال اول تا چهارم) بالای صفحه — YearDisplayUI.cs، تست شد
☑ نمایش عدد شاخص‌ها رو خودِ نوارها — StatBarUI با کد خودش می‌سازه، تست شد
☑ دکمه‌ی «دیدن تبلیغ» موقع باخت — فقط شبیه‌سازی (بدون SDK واقعی)، شاخص بحرانی رو به ۳۰ برمی‌گردونه و ادامه می‌ده، تست شد. اتصال SDK واقعی تپسل/ادیوری هنوز کار جداییه.
☐ صفحات سالانه‌ی جزئی‌تر (گزارش هر ۱۲ ماه با محتوای اختصاصی، تست نیمه‌راه ماه ۲۴) — فرق داره با نمایش ساده‌ی «سال جاری» که بالا اضافه شد
☑ گرافیک/UI اولیه (تم میز ریاست‌جمهوری) — پس‌زمینه‌ی منو و گیم‌پلی، لوگو، آیکون ۴ شاخص، دکمه‌ی «شروع بازی» و «بازگشت» وصل شدن. هنوز تو Editor تست نشده (بخش ۶). طرح روی کارت (Card cover) عمداً وصل نشد — بخش ۶ رو بخون.
☐ آیکون اپلیکیشن (App Icon نهایی برای انتشار)، اسپلش‌اسکرین، خروجی نهایی APK
```

### سند طراحی اصلی (خلاصه، برای مرجع)
- ۱۰۰ کارت هدف‌گذاری شده: ۶۰٪ عادی، ۲۰٪ بحرانی، ۱۵٪ زنجیره‌ای/داستانی، ۵٪ ویژه مناسبتی
- ۸ نوع باخت (هر شاخص می‌تونه از دو طرف باخت بده — مثلاً بودجه صفر = ورشکستگی، بودجه صد = ابرتورم)
- سیستم درآمدزایی: تبلیغ جایزه‌دار به اسم «حکم حکومتی / وتوی ریاست‌جمهوری» — موقع باخت، دیدن تبلیغ شاخص بحرانی رو به ۳۰٪ برمی‌گردونه (حداکثر ۱ بار در هر ۴ سال)
- ایستگاه‌های سالانه: ماه ۱۲ (گزارش ۱۰۰ روزه)، ماه ۲۴ (تست نیمه‌راه)، ماه ۳۶ (بحران‌های سال پایانی)، ماه ۴۸ (پیروزی)

---

## ۵. نکات کاری با این پروژه

- توسعه‌دهنده معمولاً با اسکرین‌شات از خطاهای Unity Console کار می‌کرده؛ حالا که تو (Claude Code) دسترسی مستقیم به فایل‌ها داری، لازم نیست منتظر اسکرین‌شات بمونی — مستقیم فایل رو بخون.
- بعد از هر تغییر مهم، به توسعه‌دهنده یادآوری کن که تو خودِ Unity Editor سیو کنه (Ctrl+S) و یه بکاپ از پوشه‌ی پروژه بگیره — یه بار کل صحنه رو گم کرده بودیم.
- قبل از تغییر معماری اصلی (مثلاً عوض کردن الگوی Singleton، یا تغییر نحوه‌ی نمایش کارت از Sprite به UI)، حتماً اول توضیح بده چرا و منتظر تایید بمون — این تصمیم‌ها آگاهانه گرفته شدن.
- کدها همه کامنت فارسی دارن برای بخش‌های مهم؛ همین سبک رو ادامه بده.

---

## ۶. قدم بعدی پیشنهادی

حلقه‌ی کامل بازی، Indicator Hint، و صفحه‌ی پایان بازی همه پیاده‌سازی، وایر، و تو Editor تست شدن — کار می‌کنن.

منوی اصلی و ذخیره‌سازی هم پیاده‌سازی، وایر، و تو Editor تست شدن — کار می‌کنن.

عدد شاخص‌ها، نمایش سال، و دکمه‌ی تبلیغ هم پیاده‌سازی، وایر، و تو Editor تست شدن — کار می‌کنن.

گرافیک اولیه (پس‌زمینه، لوگو، آیکون‌ها، دکمه‌ها) هم وصل و تست شد.

### وضعیت تازه‌ترین کار (هنوز تست نشده) — رفع سه باگ بعد از اضافه‌شدن گرافیک
توسعه‌دهنده بعد از تست گرافیک سه مشکل گزارش کرد؛ هر سه **فقط با تغییر کد** (بدون دستکاری فایل صحنه، پس بدون ریسک خرابی صحنه) حل شدن:

1. **«کارت نمایش داده نمیشه»** → علت: پس‌زمینه‌ی BGGame که به‌صورت UI Image روی Canvasِ Screen Space - Overlay بود، کارت (SpriteRenderer دنیایی) رو کامل می‌پوشوند. `BackgroundUI.cs` بازنویسی شد تا پس‌زمینه یه SpriteRenderer توی دنیا با sortingOrder=-100 باشه (پشت کارت). حالا کارت باید دیده بشه.
2. **«شاخص‌ها مزخرفن، باید track واسه اسلایدر بسازیم، اینا فقط آیکونن»** → `StatBarUI.StyleSlider()` اضافه شد که اسلایدر سفیدِ پیش‌فرض رو تم‌دار می‌کنه: track چرمی تیره، fill طلایی، دستگیره مخفی. (اگه بعد از تست بازم خواستی از خودِ اسپرایت‌های چرمی مثل Strip.png استفاده کنیم به‌جای رنگ ساده، بگو.)
3. **«تایمر: فقط سال اول زدی، ماه‌به‌ماه بشمر»** → `YearDisplayUI` حالا سال + ماهِ شمسی نشون می‌ده («سال اول — فروردین») و ماه‌به‌ماه جلو می‌ره.

**بعد از Pull، تو Unity این سه‌تا رو چک کن:** کارت دیده می‌شه؟ نوارها تم‌دار شدن (چرمی/طلایی)؟ بالای صفحه سال+ماه درست ماه‌به‌ماه جلو می‌ره؟

---

### گرافیک/UI تم میز ریاست‌جمهوری (قبلاً وصل و تست شد)
Assetهای گرافیکی (پوشه‌ی `Assets/Art`) بررسی و وصل شدن:

**نکته‌ی فنی مهم:** این فایل‌های عکس با **Git LFS** ذخیره می‌شن (طبق `.gitattributes` پروژه). تو این محیط، git-lfs از قبل نصب نبود؛ نصبش کردم و `git lfs pull` زدم تا محتوای واقعی عکس‌ها (نه فقط pointer file) دانلود بشه. **اگه یه روز تو کامپیوتر خودت با git/GitHub Desktop عکس‌ها رو باز کردی و بازم خراب/خالی بودن، یعنی Git LFS رو کامپیوترت نصب نیست** — از https://git-lfs.com نصبش کن (یا از تنظیمات GitHub Desktop) و یه بار `git lfs pull` بزن.

**چیزهایی که وصل شدن:**
- `BG.png` → پس‌زمینه‌ی منوی اصلی (`MainMenuUI.backgroundSprite`)
- `BGGame.png` → پس‌زمینه‌ی ثابت گیم‌پلی، پشت همه‌چیز (اسکریپت جدید `BackgroundUI.cs`)
- `Game logo.jpg` → لوگو تو منوی اصلی، به‌جای متن ساده‌ی «دولت شو»
- `Economic indicator.png` / `Popularity index.png` / `Security Index.png` / `Diplomacy Index.png` → آیکون کنار هر کدوم از ۴ نوار وضعیت
- از داخل `ButtonAssets.png` (یه اطلس چندتایی که خودت تو Unity برش زده بودی): دکمه‌ی «شروع بازی» برای New Game تو منو، و دکمه‌ی «بازگشت» برای «بازگشت به منو» تو صفحه‌ی باخت

**`Card cover.png`** — توسعه‌دهنده خودش تو Unity این رو به SpriteRenderer کارت وصل کرد (متاش رو به spriteMode: 1 تغییر داد و Scale/Collider کارت رو دستی تنظیم کرد). حالا اسپرایت کارت همینه. یه Animator/Animation Controller (`Card cover_0.controller` + `New Animation.anim`) هم اضافه کرده.

اگه بعد از تست همه‌چی درست بود، قدم بعدی: نوشتن محتوای واقعی کارت‌ها، یا اتصال SDK واقعی تبلیغ.

---

## ۷. داستانِ بازی — فروردینِ سال اول (کمپینِ انتخابات)

**قابِ داستان:** بازیکن کاندیدای ریاست‌جمهوریه و فروردین ماهِ کمپینه. هر کارت یه مشکلِ واقعیِ ایرانه که باید موضع بگیره. **لحن: طنزِ سیاسیِ نیش‌دار** (هم‌خون با اسمِ «دولت شو») — محترمانه، بدونِ توهینِ مستقیم به شخصِ حقیقی.

**۱۲ کارت (به‌ترتیب، تو `Resources/Story/Farvardin/`):**
1. `F01_Inflation` — تورم و کنترلِ قیمت با فرمان
2. `F02_Subsidy` — سه‌برابر کردنِ یارانه‌ی نقدی
3. `F03_Sanctions` — مذاکره با غرب یا مقاومت
4. `F04_Internet` — رفعِ فیلترِ اینترنت
5. `F05_Water` — طرحِ گران‌قیمتِ انتقالِ آب
6. `F06_Housing` — وعده‌ی میلیون‌ها مسکن
7. `F07_BrainDrain` — مهاجرتِ نخبگان
8. `F08_CarImport` — آزادسازیِ وارداتِ خودرو
9. `F09_Pensions` — همسان‌سازیِ حقوقِ بازنشسته‌ها
10. `F10_SocialFreedom` — سبکِ زندگی/آزادی‌های اجتماعی
11. `F11_Corruption` — افشای فسادِ متحدها
12. `F12_Power` — قولِ پایانِ خاموشیِ برق

**الگوی اثرها:** تصمیم‌های پوپولیستی معمولاً محبوبیت↑ ولی بودجه/امنیت/دیپلماسی↓؛ تصمیم‌های مسئولانه برعکس. بازیِ افراطی (همه پوپولیست یا همه ولخرج) ممکنه یه شاخص رو به ۰/۱۰۰ برسونه و **باختِ زودهنگام** بده (عمدیه، مثل Reigns). اعداد تو فایل‌های .asset قابلِ تنظیمن.

**۴ پایانِ ماه** (تو `MonthReportUI`، بر اساسِ وضعیتِ نهایی): قهرمانِ ملی / پوپولیستِ محبوب / تکنوکراتِ بی‌رأی / کاندیدای فراموش‌شده. شاخه‌ها فقط مالِ همین ماهن و به ماهِ بعد سرایت نمی‌کنن.

**چیزهایی که هنوز مونده (برای این ماه):**
- **عکس‌ها:** برای هر کارت (`illustration`) و هر پایان (`Resources/Story/Farvardin/Endings/ending_0..3`) هنوز تصویری ساخته نشده — فیلدها آماده‌ن، فقط باید عکس‌ها تولید و وصل بشن (پرامپت‌ها به توسعه‌دهنده داده شد).
- **تستِ Editor:** هنوز تو Unity اجرا/تست نشده.
- **سیو دوباره فعال شد (✅):** `storyIndex` حالا تو `SaveData` ذخیره می‌شه، پس «ادامه بازی قبلی» از همون کارتی که بازیکن بود ادامه می‌ده (نه از اولِ ماه). تو `CardSwipe.ApplyCardEffects` بعد از هر تصمیم سیو می‌شه؛ موقعِ باخت یا کاملْ‌شدنِ ماه (`EndStoryMonth`) سیو پاک می‌شه. مکانیزم ادامه: `CardDatabase.SetResumeStoryIndex(idx)` → `StartStoryMonth` از اون کارت شروع می‌کنه.
  - **نکته:** چون قبلاً `DontDestroyOnLoad` از `GameStats` حذف شد (بازیِ تک‌صحنه‌ست)، بازگشت به منو با ری‌لودِ صحنه درست کار می‌کنه و شاخص‌ها هم تازه می‌شن.
- **محدودیتِ باقی‌مانده‌ی سیو:** تاریخچه‌ی ۵ کارتِ اخیر (`recentCardIds`، فقط برای ماه‌های تصادفیِ آینده) تو سیو ذخیره نمی‌شه — تو حالتِ داستانی بی‌اثره.

**قدم‌های بعدی داستان:** تولید و اتصالِ عکسِ کارت‌ها و پایان‌ها؛ بعد نوشتنِ ماهِ اردیبهشت (۱۲ کارتِ خودش تو `Resources/Story/Ordibehesht/` — ولی برای ماه‌های تصادفی/غیرداستانی باید `storyResourcesPath` عوض بشه یا منطقِ چندماهه اضافه بشه).
